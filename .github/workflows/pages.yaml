# yaml-language-server: $schema=https://raw.githubusercontent.com/SchemaStore/schemastore/refs/heads/master/src/schemas/json/github-workflow.json
name: pages
on:
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "mise.toml"
      - "mise.lock"
      - "taskfile.yaml"
      - ".github/workflows/pages.yaml"
  workflow_dispatch: {}

env:
  MISE_GITHUB_TOKEN: ${{ github.token }}
  MISE_AUTO_INSTALL: "0"

# Allow the GITHUB_TOKEN to deploy to GitHub Pages.
permissions:
  contents: read
  pages: write
  id-token: write

# Allow only one concurrent deployment; don't cancel an in-progress production deploy.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
      # The terminal snippets (docs/assets/terminals) and screenshots
      # (docs/static/images) are committed and regenerated by hand
      # (`task docs:terminals`, `task screenshots`), so the site builds from
      # go + extended Hugo alone — no Rust toolchain, no micromux binary.
      - uses: jdx/mise-action@v4
        with:
          version: 2026.7.16
          install: false
          cache: true
          env: false
      - name: Install docs toolchain
        run: >-
          mise install
          go "asdf:NeoHsu/asdf-hugo" task
      - name: Configure Pages
        id: pages
        uses: actions/configure-pages@v6
      - name: Build docs
        run: task docs:build -- --baseURL "${{ steps.pages.outputs.base_url }}/"
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: docs/public

  deploy:
    needs: build
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
